'use strict';

describe('Directive: stations', function () {

  // load the directive's module
  beforeEach(module('bikespotterApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
});
